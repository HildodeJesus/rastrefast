<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RastreFast - Favoritados</title>
  <link rel="stylesheet" href="css/default.css">
  <link rel="stylesheet" href="css/historicPage.css">
  <link rel="stylesheet" href="css/header.css">
  <link rel="stylesheet" href="css/footer.css">
</head>
<body>
  <%- include('partials/header.ejs') %>

  <main>
    <div class="container">
      <section id="historic">
        <h1>Seus Favoritos</h1>
        <hr>
        <ul id="list--historic">
          <% if(historic.length > 0) { %>
            <% historic.forEach(item => { %>
              <li class="item-list--historic">
                <div id="item-rigth">
                  <a href="tracker?code=<%= item.codeOrder%>"><%= item.name %></a>
                  <span><%= item.codeOrder %></span>
                </div>
                <div id="item-left">
                  <span class="trash">
                    <a href="/api/deleteOrder/<%= item.id %>">🗑️</a> 
                  </span>
                </div>
              </li>
            <% }) %>
          <% } else { %>  
            <li>Não há nada salvo</li>
          <% } %>
        </ul>
      </section>
    </div>
    
  </main>

  <%- include('partials/footer.ejs') %>
</body>
</html>